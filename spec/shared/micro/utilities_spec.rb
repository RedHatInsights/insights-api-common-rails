RSpec.describe Shared::Micro::Utilities do
  it "has a version number" do
    expect(Shared::Micro::Utilities::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
